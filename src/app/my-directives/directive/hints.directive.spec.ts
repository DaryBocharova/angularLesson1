import { HintsDirective } from './hints.directive';

describe('HintsDirective', () => {
  it('should create an instance', () => {
    const directive = new HintsDirective();
    expect(directive).toBeTruthy();
  });
});
